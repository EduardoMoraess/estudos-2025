/*const btn = document.getElementById('btn'); // Seleciona o botão pelo ID

// Define o emoji
let emoji = '\u{1F389}';

// Adiciona o evento de clique ao botão
btn.addEventListener('click', function () {
    alert(`Valeu pelo like ${emoji}`); // Exibe o emoji em um alert
});
*/

function like(){
    let emoji = '\u{1F389}'
    alert(`Valeu pelo like ${emoji}`)
}
